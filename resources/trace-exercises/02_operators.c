/**
 * 程式追蹤練習 02：運算子追蹤
 * 難度：⭐
 * 
 * 練習方式：
 * 1. 在紙上畫出變數值追蹤表格
 * 2. 注意運算子的優先順序
 * 3. 預測最終輸出
 * 4. 執行程式驗證
 */

#include <stdio.h>

int main() {
    printf("=== 題目 1：整數除法 ===\n");
    {
        int a = 7, b = 2;
        int c = a / b;
        int d = a % b;
        printf("c=%d, d=%d\n", c, d);
    }
    
    printf("\n=== 題目 2：混合運算 ===\n");
    {
        int x = 10;
        x += 5;
        x -= 3;
        x *= 2;
        x /= 4;
        printf("x=%d\n", x);
    }
    
    printf("\n=== 題目 3：自增自減 ===\n");
    {
        int a = 5;
        int b = a++;
        int c = ++a;
        printf("a=%d, b=%d, c=%d\n", a, b, c);
    }
    
    printf("\n=== 題目 4：型態轉換 ===\n");
    {
        int a = 5, b = 2;
        double c = a / b;
        double d = (double)a / b;
        printf("c=%.1f, d=%.1f\n", c, d);
    }
    
    printf("\n=== 題目 5：優先順序 ===\n");
    {
        int a = 2, b = 3, c = 4;
        int r1 = a + b * c;
        int r2 = (a + b) * c;
        int r3 = a * b + c * a;
        printf("r1=%d, r2=%d, r3=%d\n", r1, r2, r3);
    }
    
    printf("\n=== 題目 6：邏輯運算 ===\n");
    {
        int a = 5, b = 0, c = -3;
        int r1 = a && b;
        int r2 = a || b;
        int r3 = !b;
        int r4 = a && c;
        printf("r1=%d, r2=%d, r3=%d, r4=%d\n", r1, r2, r3, r4);
    }
    
    printf("\n=== 題目 7：比較運算 ===\n");
    {
        int a = 10, b = 20;
        int r1 = (a > b);
        int r2 = (a <= b);
        int r3 = (a == 10);
        int r4 = (a != b);
        printf("r1=%d, r2=%d, r3=%d, r4=%d\n", r1, r2, r3, r4);
    }
    
    printf("\n=== 題目 8：短路求值 ===\n");
    {
        int x = 0;
        int a = 5;
        // && 短路：左邊是 false 就不看右邊
        int r1 = (x != 0) && (a / x > 2);
        printf("r1=%d (安全，不會除以零)\n", r1);
        
        // || 短路：左邊是 true 就不看右邊
        int r2 = (a > 0) || (a / x > 2);
        printf("r2=%d (安全，不會除以零)\n", r2);
    }
    
    return 0;
}

/*
 * ========================================
 * 解答（先自己追蹤，再看答案！）
 * ========================================
 *
 * 題目 1：
 * 7 / 2 = 3（整數除法，捨棄小數）
 * 7 % 2 = 1（餘數）
 * 輸出：c=3, d=1
 *
 * 題目 2 追蹤表：
 * | 步驟 | x |
 * |------|---|
 * | 初始 | 10 |
 * | x+=5 | 15 |
 * | x-=3 | 12 |
 * | x*=2 | 24 |
 * | x/=4 | 6 |
 * 輸出：x=6
 *
 * 題目 3 追蹤表：
 * | 步驟 | a | b | c |
 * |------|---|---|---|
 * | 初始 | 5 | - | - |
 * | b=a++ | 6 | 5 | - | (先取 a 的值給 b，再 a+1)
 * | c=++a | 7 | 5 | 7 | (先 a+1，再取值給 c)
 * 輸出：a=7, b=5, c=7
 *
 * 題目 4：
 * c = 5/2 = 2（先做整數除法得 2，再轉 double）
 * d = 5.0/2 = 2.5（先轉 double 再除）
 * 輸出：c=2.0, d=2.5
 *
 * 題目 5：
 * r1 = 2 + (3*4) = 14
 * r2 = (2+3) * 4 = 20
 * r3 = (2*3) + (4*2) = 6 + 8 = 14
 * 輸出：r1=14, r2=20, r3=14
 *
 * 題目 6：
 * r1 = 5 && 0 = 0（有一個 false）
 * r2 = 5 || 0 = 1（有一個 true）
 * r3 = !0 = 1
 * r4 = 5 && (-3) = 1（非零都是 true）
 * 輸出：r1=0, r2=1, r3=1, r4=1
 *
 * 題目 7：
 * r1 = (10 > 20) = 0
 * r2 = (10 <= 20) = 1
 * r3 = (10 == 10) = 1
 * r4 = (10 != 20) = 1
 * 輸出：r1=0, r2=1, r3=1, r4=1
 *
 * 題目 8：
 * r1：x==0 所以 (x!=0) 是 false，&& 短路，不計算右邊
 *     結果：r1 = 0
 * r2：a==5 所以 (a>0) 是 true，|| 短路，不計算右邊
 *     結果：r2 = 1
 * 重點：短路求值避免了除以零的危險！
 */
