# Lab 09 Part 2: 記憶體分析答案

閱讀 `src/memory.c`，回答以下問題。

---

## 問題 1：sizeof 分析

假設 `sizeof(int)` 為 4 bytes，`sizeof(int*)` 為 8 bytes：

- `sizeof(a)` 的值是？

```
答：
```

- `sizeof(a[0])` 的值是？

```
答：
```

- `sizeof(a) / sizeof(a[0])` 的值是？

```
答：
```

---

## 問題 2：初始化分析

- `b[3] = {1, 2}` 中，`b[2]` 的值是什麼？為什麼？

```
答：
```

- `c[5] = {0}` 中，所有元素的值是什麼？為什麼？

```
答：
```

---

## 問題 3：位址分析

假設 `a[0]` 的位址是 `0x1000`：

| 元素 | 位址 |
|-----|------|
| a[0] | 0x1000 |
| a[1] | |
| a[2] | |
| a[3] | |
| a[4] | |

每個元素之間相差幾個 bytes？為什麼？

```
答：
```

---

## 問題 4：函式傳遞

- `modify` 函式中 `sizeof(arr)` 的值是多少？

```
答：
```

- 為什麼和 main 中的 `sizeof(a)` 不同？

```
答：
```

- `modify(a, 5)` 執行後，main 中 a 的值有沒有變？為什麼？

```
答：
```

---

## 問題 5：思考題

如果宣告 `int x = 99;` 且 `x` 恰好放在 `a[5]` 的位址，那麼 `a[5] = 0;` 會發生什麼事？

```
答：
```
