/**
 * Lab 06 Part 2: 迴圈除錯
 * 
 * 每個迴圈都有 bug，請找出並修正。
 */

#include <stdio.h>

int main() {
    // ========================================
    // Bug 1：計算 1 到 10 的總和
    // 預期結果：55
    // ========================================
    printf("=== Bug 1 ===\n");
    
    int sum1 = 0;
    for (int i = 1; i < 10; i++) {  // Bug 在這裡
        sum1 += i;
    }
    printf("1 到 10 的總和 = %d (應該是 55)\n", sum1);
    
    // ========================================
    // Bug 2：印出 5 4 3 2 1
    // ========================================
    printf("\n=== Bug 2 ===\n");
    
    for (int i = 5; i > 0; i++) {  // Bug 在這裡
        printf("%d ", i);
    }
    printf("(應該印出 5 4 3 2 1)\n");
    
    // ========================================
    // Bug 3：計算 5 的階乘
    // 預期結果：120
    // ========================================
    printf("\n=== Bug 3 ===\n");
    
    int factorial = 0;  // Bug 在這裡
    for (int i = 1; i <= 5; i++) {
        factorial *= i;
    }
    printf("5! = %d (應該是 120)\n", factorial);
    
    // ========================================
    // Bug 4：印出偶數
    // 預期：2 4 6 8 10
    // ========================================
    printf("\n=== Bug 4 ===\n");
    
    for (int i = 1; i <= 10; i++) {
        if (i % 2 == 0); {  // Bug 在這裡
            printf("%d ", i);
        }
    }
    printf("(應該只印出 2 4 6 8 10)\n");
    
    // ========================================
    // Bug 5：找出第一個大於 100 的平方數
    // ========================================
    printf("\n=== Bug 5 ===\n");
    
    int i = 1;
    while (i * i < 100) {
        i++;
    }
    // Bug：條件判斷有問題
    printf("第一個大於 100 的平方數是 %d (i=%d)\n", i*i, i);
    printf("(應該是 121，i=11)\n");
    
    return 0;
}

/*
 * 請在下方說明每個 bug 的問題和修正方式：
 *
 * Bug 1:
 * 問題：
 * 修正：
 *
 * Bug 2:
 * 問題：
 * 修正：
 *
 * Bug 3:
 * 問題：
 * 修正：
 *
 * Bug 4:
 * 問題：
 * 修正：
 *
 * Bug 5:
 * 問題：
 * 修正：
 */
