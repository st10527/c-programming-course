# Lab 08 Part 2: 遞迴追蹤答案

請追蹤 `src/trace.c` 中的遞迴函式。

---

## 問題 1：mystery_a(5) 呼叫樹

畫出 `mystery_a(5)` 的完整呼叫樹：

```
mystery_a(5)
├── mystery_a(4)
│   ├── mystery_a(3)
│   │   ├── mystery_a(2)
│   │   │   ├── mystery_a(1) → ?
│   │   │   └── mystery_a(0) → ?
│   │   └── ...
│   └── ...
└── ...

（請完成整棵呼叫樹，標出每個節點的回傳值）
```

`mystery_a(5)` 的回傳值是？

```
答：
```

你認出這是什麼數列了嗎？

```
答：
```

---

## 問題 2：mystery_b(1234) 追蹤

寫出 `mystery_b(1234)` 的每一步遞迴呼叫：

```
mystery_b(1234)
  → mystery_b(___) + ___
    → mystery_b(___) + ___
      → mystery_b(___) + ___
        → ___ (base case)
```

最終回傳值：

```
答：
```

這個函式的功能是什麼？

```
答：
```

---

## 問題 3：is_even(4) 追蹤

追蹤 `is_even(4)` 的呼叫鏈：

```
is_even(4) → is_odd(___) → is_even(___) → ...
```

回傳值：

```
答：
```

---

## 問題 4：is_odd(3) 追蹤

追蹤 `is_odd(3)` 的呼叫鏈：

```
is_odd(3) → is_even(___) → is_odd(___) → ...
```

回傳值：

```
答：
```

---

## 問題 5：程式的完整輸出

```
（在此寫出完整輸出）


```
