# Lab 04 Part 2: 程式碼品質比較答案

請分析每組寫法，說明哪種較好以及為什麼。

---

## 比較 1：判斷偶數

**寫法 A**：
```c
if (x % 2 == 0) {
    printf("偶數\n");
} else {
    printf("奇數\n");
}
```

**寫法 B**：
```c
if (x % 2 != 0) {
    printf("奇數\n");
} else {
    printf("偶數\n");
}
```

**哪種較好？**（A / B / 一樣好）



**理由**：



---

## 比較 2：返回布林值

**寫法 A**：
```c
if (age >= 18) {
    is_adult = 1;
} else {
    is_adult = 0;
}
```

**寫法 B**：
```c
is_adult = (age >= 18);
```

**哪種較好？**（A / B / 一樣好）



**理由**：



---

## 比較 3：多重條件

**寫法 A**：巢狀 if
```c
if (score >= 60) {
    if (score >= 80) {
        printf("優秀\n");
    } else {
        printf("及格\n");
    }
} else {
    printf("不及格\n");
}
```

**寫法 B**：else if 串接
```c
if (score >= 80) {
    printf("優秀\n");
} else if (score >= 60) {
    printf("及格\n");
} else {
    printf("不及格\n");
}
```

**哪種較好？**（A / B / 一樣好）



**理由**：



---

## 比較 4：條件邏輯方向

**寫法 A**：正向判斷
```c
if (n > 0) {
    // 正數處理...
} else {
    printf("非正數，跳過\n");
}
```

**寫法 B**：先排除不符條件的情況（Guard Clause）
```c
if (n <= 0) {
    printf("非正數，跳過\n");
    return;  // 或 continue
}
// 正數處理...
```

**哪種較好？**（A / B / 看情況）



**理由**：



**什麼情況下 Guard Clause 特別有用？**



---

## 比較 5：閏年判斷

**寫法 A**：巢狀判斷（約 12 行）

**寫法 B**：邏輯運算子（1 行）
```c
is_leap = (year % 400 == 0) || (year % 100 != 0 && year % 4 == 0);
```

**哪種較好？**（A / B / 看情況）



**理由**：



**什麼時候簡潔不一定是好的？**



---

## 總結

根據以上分析，好的條件判斷程式碼應該具備什麼特質？

1. 

2. 

3. 
