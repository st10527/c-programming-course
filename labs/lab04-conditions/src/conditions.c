/**
 * Lab 04 Part 1: 條件判斷練習
 * 
 * 完成每個 TODO，實作指定的功能。
 */

#include <stdio.h>

int main() {
    // ========================================
    // 練習 1：成績等第
    // 90+ = A, 80-89 = B, 70-79 = C, 60-69 = D, <60 = F
    // ========================================
    printf("=== 練習 1：成績等第 ===\n");
    
    int score = 85;
    char grade;
    
    // TODO: 使用 if-else if-else 判斷成績等第
    // 將結果存入 grade 變數
    
    
    printf("分數 %d 的等第是: %c\n", score, grade);
    // 預期輸出：分數 85 的等第是: B
    
    // ========================================
    // 練習 2：閏年判斷
    // 閏年規則：
    // - 能被 400 整除 → 閏年
    // - 能被 100 整除但不能被 400 整除 → 平年
    // - 能被 4 整除 → 閏年
    // - 其他 → 平年
    // ========================================
    printf("\n=== 練習 2：閏年判斷 ===\n");
    
    int year = 2024;
    int is_leap_year = 0;  // 1 = 閏年, 0 = 平年
    
    // TODO: 實作閏年判斷邏輯
    
    
    if (is_leap_year) {
        printf("%d 是閏年\n", year);
    } else {
        printf("%d 是平年\n", year);
    }
    // 預期輸出：2024 是閏年
    
    // ========================================
    // 練習 3：三個數的最大值
    // ========================================
    printf("\n=== 練習 3：找最大值 ===\n");
    
    int a = 15, b = 42, c = 28;
    int max;
    
    // TODO: 找出 a, b, c 中的最大值，存入 max
    
    
    printf("最大值是: %d\n", max);
    // 預期輸出：最大值是: 42
    
    // ========================================
    // 練習 4：簡化條件式
    // 用更簡潔的方式改寫
    // ========================================
    printf("\n=== 練習 4：簡化條件式 ===\n");
    
    int age = 20;
    int is_adult;
    
    // 原本的寫法（冗長）：
    // if (age >= 18) {
    //     is_adult = 1;
    // } else {
    //     is_adult = 0;
    // }
    
    // TODO: 用一行完成同樣的功能
    // is_adult = ???;
    
    
    printf("is_adult = %d\n", is_adult);
    // 預期輸出：is_adult = 1
    
    // ========================================
    // 練習 5：範圍檢查
    // 檢查數字是否在 1-100 之間（包含邊界）
    // ========================================
    printf("\n=== 練習 5：範圍檢查 ===\n");
    
    int number = 50;
    int in_range;
    
    // TODO: 使用 && 運算子檢查 number 是否在 1-100 之間
    // in_range = ???;
    
    
    if (in_range) {
        printf("%d 在範圍內\n", number);
    } else {
        printf("%d 不在範圍內\n", number);
    }
    // 預期輸出：50 在範圍內
    
    return 0;
}
