# Lab 07 Part 2: 函式追蹤答案

請追蹤 `src/trace.c` 的執行過程。

---

## 問題 1：Call Stack 變化

追蹤 `compute(3)` 的執行。當 `i=1` 時，`multiply(1, 1)` 被呼叫，接著 `add(0, 1)` 被呼叫。

此時 Call Stack 由底到頂是什麼？

```
答：（由底到頂列出函式名稱）


```

---

## 問題 2：函式呼叫順序

列出所有 `add` 函式的呼叫（依序），包含參數值：

```
第 1 次：add(__, __)
第 2 次：add(__, __)
第 3 次：add(__, __)
第 4 次：add(__, __)
第 5 次：add(__, __)
第 6 次：add(__, __)
...（如果還有更多，請繼續列出）
```

---

## 問題 3：逐步追蹤

追蹤主迴圈每次迭代的值：

| 迭代 (i) | multiply(i, i) 的結果 (sq) | sum 的變化 |
|----------|---------------------------|-----------|
| 1 | | |
| 2 | | |
| 3 | | |

---

## 問題 4：最終結果

`compute(3)` 的回傳值是？

```
答：
```

（提示：1² + 2² + 3² = ?）

---

## 問題 5：程式的完整輸出

請寫出執行這段程式後的完整輸出：

```
（在此寫出完整輸出）


```
