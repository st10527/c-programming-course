/**
 * Lab 07 Part 1: 函式練習
 */

#include <stdio.h>

// ========================================
// 練習 1：計算絕對值
// ========================================

// TODO: 完成函式 my_abs
// 參數：一個整數 n
// 回傳：n 的絕對值
int my_abs(int n) {
    // 請在此完成
    return 0;  // 請修改
}

// ========================================
// 練習 2：計算次方
// ========================================

// TODO: 完成函式 power
// 參數：底數 base，指數 exp（exp >= 0）
// 回傳：base 的 exp 次方
// 例如：power(2, 3) 回傳 8
int power(int base, int exp) {
    // 請在此完成
    return 0;  // 請修改
}

// ========================================
// 練習 3：判斷質數
// ========================================

// TODO: 完成函式 is_prime
// 參數：一個正整數 n
// 回傳：如果 n 是質數回傳 1，否則回傳 0
// 注意：1 不是質數，2 是最小的質數
int is_prime(int n) {
    // 請在此完成
    return 0;  // 請修改
}

// ========================================
// 練習 4：計算最大公因數 (GCD)
// ========================================

// TODO: 完成函式 gcd
// 參數：兩個正整數 a, b
// 回傳：a 和 b 的最大公因數
// 提示：使用輾轉相除法
//   gcd(a, b) = gcd(b, a % b)，當 b == 0 時回傳 a
int gcd(int a, int b) {
    // 請在此完成
    return 0;  // 請修改
}

// ========================================
// 練習 5：印出 N 個星號
// ========================================

// TODO: 完成函式 print_stars
// 參數：一個正整數 n
// 功能：印出 n 個 '*'，最後印換行
// 例如：print_stars(5) 印出 "*****\n"
void print_stars(int n) {
    // 請在此完成
}

// ============================================================

int main() {
    // 測試練習 1
    printf("=== 練習 1：絕對值 ===\n");
    printf("my_abs(-5) = %d\n", my_abs(-5));    // 預期：5
    printf("my_abs(3) = %d\n", my_abs(3));      // 預期：3
    printf("my_abs(0) = %d\n", my_abs(0));      // 預期：0
    
    // 測試練習 2
    printf("\n=== 練習 2：次方 ===\n");
    printf("power(2, 3) = %d\n", power(2, 3));    // 預期：8
    printf("power(5, 0) = %d\n", power(5, 0));    // 預期：1
    printf("power(3, 4) = %d\n", power(3, 4));    // 預期：81
    
    // 測試練習 3
    printf("\n=== 練習 3：質數判斷 ===\n");
    printf("is_prime(2) = %d\n", is_prime(2));    // 預期：1
    printf("is_prime(7) = %d\n", is_prime(7));    // 預期：1
    printf("is_prime(10) = %d\n", is_prime(10));  // 預期：0
    printf("is_prime(1) = %d\n", is_prime(1));    // 預期：0
    
    // 測試練習 4
    printf("\n=== 練習 4：最大公因數 ===\n");
    printf("gcd(12, 8) = %d\n", gcd(12, 8));      // 預期：4
    printf("gcd(15, 25) = %d\n", gcd(15, 25));    // 預期：5
    printf("gcd(7, 3) = %d\n", gcd(7, 3));        // 預期：1
    
    // 測試練習 5
    printf("\n=== 練習 5：印星號 ===\n");
    print_stars(5);   // 預期：*****
    print_stars(3);   // 預期：***
    print_stars(10);  // 預期：**********
    
    return 0;
}
