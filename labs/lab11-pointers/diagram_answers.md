# Lab 11 Part 2: 記憶體圖答案

閱讀 `src/diagrams.c`，畫出每個階段的記憶體圖。

---

## 階段 1

```c
int x = 10;
int y = 20;
int *p = &x;
int *q = &y;
```

畫出此時的記憶體圖（用箭頭表示指標指向）：

```
變數  值
┌──────────┐
│ x =      │
│ y =      │
│ p → ?    │
│ q → ?    │
└──────────┘
```

---

## 階段 2

```c
*p = 30;
q = p;
```

畫出此時的記憶體圖：

```
變數  值
┌──────────┐
│ x =      │
│ y =      │
│ p → ?    │
│ q → ?    │
└──────────┘
```

問題：`*p` 現在是多少？`*q` 現在是多少？

```
答：
```

---

## 階段 3

```c
*q = 50;
```

畫出此時的記憶體圖：

```
變數  值
┌──────────┐
│ x =      │
│ y =      │
│ p → ?    │
│ q → ?    │
└──────────┘
```

問題：x 的值是多少？y 的值是多少？為什麼？

```
答：
```

---

## 階段 4：mystery 函式

```c
int a = 5, b = 8;
mystery(&a, &b);
```

`mystery` 函式的內容：
```c
void mystery(int *a, int *b) {
    *a = *a + *b;    // 步驟 1
    *b = *a - *b;    // 步驟 2
    *a = *a - *b;    // 步驟 3
}
```

逐步追蹤（寫出每一步後 main 中 a 和 b 的值）：

| 步驟 | *a (main 的 a) | *b (main 的 b) |
|-----|----------------|----------------|
| 初始 | 5 | 8 |
| 步驟 1 後 | | |
| 步驟 2 後 | | |
| 步驟 3 後 | | |

最終結果：a = ___, b = ___

這個 mystery 函式的功能是什麼？

```
答：
```

---

## 問題 5：程式最終輸出

```
（在此寫出完整輸出）


```
