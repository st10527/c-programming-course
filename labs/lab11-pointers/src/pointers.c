/**
 * Lab 11 Part 1: 指標操作
 */

#include <stdio.h>

int main() {
    // ========================================
    // 練習 1：基本指標操作
    // ========================================
    printf("=== 練習 1：基本指標操作 ===\n");
    
    int x = 10;
    int y = 20;
    
    // TODO: 宣告一個 int 指標 p，指向 x
    
    
    // TODO: 用 *p 印出 x 的值
    printf("*p = %d\n", 0);  // 請修改，預期：10
    
    // TODO: 透過 *p 將 x 的值改為 30
    
    
    printf("x = %d (應為 30)\n", x);
    
    // TODO: 讓 p 改指向 y
    
    
    // TODO: 透過 *p 將 y 的值改為 40
    
    
    printf("y = %d (應為 40)\n", y);
    
    // ========================================
    // 練習 2：指標與取址
    // ========================================
    printf("\n=== 練習 2：指標與取址 ===\n");
    
    int a = 100;
    int *p1 = &a;
    int **pp = &p1;  // 指向指標的指標
    
    // TODO: 分別用 a, *p1, **pp 三種方式印出 a 的值
    printf("a = %d\n", a);
    printf("*p1 = %d\n", 0);   // 請修改
    printf("**pp = %d\n", 0);  // 請修改
    
    // 它們的值應該都是 100
    
    // ========================================
    // 練習 3：指標與陣列
    // ========================================
    printf("\n=== 練習 3：指標與陣列 ===\n");
    
    int arr[5] = {10, 20, 30, 40, 50};
    int *ptr = arr;
    
    // TODO: 用指標走訪陣列，印出每個元素
    // 使用 *(ptr + i) 的方式
    for (int i = 0; i < 5; i++) {
        printf("arr[%d] = %d\n", i, 0);  // 請修改
    }
    // 預期：10, 20, 30, 40, 50
    
    // ========================================
    // 練習 4：指標比較
    // ========================================
    printf("\n=== 練習 4：指標比較 ===\n");
    
    int m = 5, n = 5;
    int *pm = &m, *pn = &n;
    
    // TODO: 比較 pm 和 pn 指向的「值」是否相同
    // 注意：不是比較位址，是比較 *pm 和 *pn
    if (0) {  // 請修改條件
        printf("值相同\n");
    } else {
        printf("值不同\n");
    }
    // 預期：值相同
    
    // TODO: 比較 pm 和 pn 的「位址」是否相同
    if (0) {  // 請修改條件
        printf("位址相同\n");
    } else {
        printf("位址不同\n");
    }
    // 預期：位址不同（m 和 n 是不同的變數）
    
    return 0;
}
