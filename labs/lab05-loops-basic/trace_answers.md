# Lab 05 Part 2: 迴圈追蹤答案

請手動追蹤每個迴圈，填寫表格，然後執行程式驗證。

---

## 題目 1：基本 for 迴圈

```c
int sum1 = 0;
for (int i = 1; i <= 5; i++) {
    sum1 += i;
}
```

追蹤表格：

| 迴圈次數 | i (迴圈開始時) | i <= 5? | sum1 (執行後) | i++ 後 |
|---------|---------------|---------|--------------|--------|
| 第 1 次 | 1 | | | |
| 第 2 次 | | | | |
| 第 3 次 | | | | |
| 第 4 次 | | | | |
| 第 5 次 | | | | |
| 第 6 次 | | (結束) | | |

**最終 sum1**：___

---

## 題目 2：步進值 += 3

```c
for (int i = 0; i < 10; i += 3) {
    printf("%d ", i);
}
```

追蹤：

| 次數 | i | i < 10? | 輸出 | i += 3 後 |
|-----|---|---------|------|-----------|
| 1 | 0 | | | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |

**預測輸出**：___

---

## 題目 3：遞減迴圈計算階乘

```c
int product = 1;
for (int i = 5; i >= 1; i--) {
    product *= i;
}
```

追蹤表格：

| 次數 | i | i >= 1? | product 計算 | product 值 |
|-----|---|---------|-------------|-----------|
| 1 | 5 | | 1 × 5 | |
| 2 | 4 | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |

**最終 product (5!)**：___

---

## 題目 4：while 計算位數

```c
int n = 12345;
int digits = 0;
while (n > 0) {
    n /= 10;
    digits++;
}
```

追蹤表格：

| 次數 | n (開始) | n > 0? | n /= 10 後 | digits |
|-----|---------|--------|-----------|--------|
| 1 | 12345 | | | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |
| 6 | | (結束) | | |

**最終 digits**：___

---

## 題目 5：條件累加（3 的倍數）

```c
int total = 0;
for (int i = 1; i <= 10; i++) {
    if (i % 3 == 0) {
        total += i;
    }
}
```

**1-10 中，哪些是 3 的倍數？**



**這些數的總和**：___

---

## 題目 6：break 提早結束

```c
int sum6 = 0;
for (int i = 1; i <= 100; i++) {
    sum6 += i;
    if (sum6 > 50) {
        break;
    }
}
```

追蹤表格：

| 次數 | i | sum6 累加後 | sum6 > 50? |
|-----|---|------------|-----------|
| 1 | 1 | 1 | |
| 2 | 2 | 3 | |
| 3 | 3 | | |
| 4 | | | |
| 5 | | | |
| ... | | | |

**當 sum6 首次超過 50 時，i 等於多少？**



**此時 sum6 的值**：___

---

## Part 3: 預測輸出

### 題目 A
預測：

### 題目 B
預測（3 行）：
```

```

### 題目 C
預測：

### 題目 D（有 continue）
預測：

### 題目 E（有 break）
預測：

### 題目 F（條件一開始就不成立）
預測：

### 題目 G（do-while，條件一開始就不成立）
預測：

**題目 F 和 G 的差別是什麼？**

