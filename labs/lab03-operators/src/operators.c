/**
 * Lab 03 Part 1: 運算子練習
 * 
 * 完成每個 TODO，讓程式輸出正確的結果。
 */

#include <stdio.h>

int main() {
    // ========================================
    // 練習 1：算術運算
    // ========================================
    printf("=== 練習 1：算術運算 ===\n");
    
    int a = 15, b = 4;
    
    // TODO: 計算 a 除以 b 的商
    int quotient = 0;  // 修改這行
    
    // TODO: 計算 a 除以 b 的餘數
    int remainder = 0;  // 修改這行
    
    printf("15 / 4 = %d ... %d\n", quotient, remainder);
    // 預期輸出：15 / 4 = 3 ... 3
    
    // ========================================
    // 練習 2：複合賦值
    // ========================================
    printf("\n=== 練習 2：複合賦值 ===\n");
    
    int score = 60;
    
    // TODO: 使用 += 讓 score 增加 20
    
    // TODO: 使用 *= 讓 score 變成原來的 2 倍
    
    printf("score = %d\n", score);
    // 預期輸出：score = 160
    
    // ========================================
    // 練習 3：遞增遞減
    // ========================================
    printf("\n=== 練習 3：遞增遞減 ===\n");
    
    int x = 5;
    int y = 5;
    
    int result1 = x++;  // 先用再加
    int result2 = ++y;  // 先加再用
    
    printf("x++ 的結果: result1 = %d, x = %d\n", result1, x);
    printf("++y 的結果: result2 = %d, y = %d\n", result2, y);
    
    // ========================================
    // 練習 4：型態轉換
    // ========================================
    printf("\n=== 練習 4：型態轉換 ===\n");
    
    int m = 5, n = 2;
    
    // TODO: 計算 m / n，但要得到浮點數結果 (2.5)
    double result = 0.0;  // 修改這行，使用型態轉換
    
    printf("5 / 2 = %.1f\n", result);
    // 預期輸出：5 / 2 = 2.5
    
    // ========================================
    // 練習 5：判斷奇偶
    // ========================================
    printf("\n=== 練習 5：判斷奇偶 ===\n");
    
    int num = 17;
    
    // TODO: 使用 % 運算子判斷 num 是奇數還是偶數
    // 如果 num % 2 == 0，是偶數；否則是奇數
    int is_even = 0;  // 修改這行：偶數設為 1，奇數設為 0
    
    if (is_even) {
        printf("%d 是偶數\n", num);
    } else {
        printf("%d 是奇數\n", num);
    }
    // 預期輸出：17 是奇數
    
    return 0;
}
