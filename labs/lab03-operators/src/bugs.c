/**
 * Lab 03 Part 3: 找出運算錯誤
 * 
 * 這個程式有幾個運算相關的 bug，請找出並修正。
 * 修正後程式應該輸出正確的結果。
 */

#include <stdio.h>

int main() {
    printf("=== 找出運算錯誤 ===\n\n");
    
    // ========================================
    // Bug 1：計算平均值
    // 預期：三個數的平均值應該是 20.0
    // ========================================
    int num1 = 10, num2 = 20, num3 = 30;
    double average = num1 + num2 + num3 / 3;
    printf("Bug 1: 平均值 = %.1f (應該是 20.0)\n", average);
    
    // ========================================
    // Bug 2：計算折扣後價格
    // 原價 100 元，打 8 折，預期結果應該是 80.0
    // ========================================
    int price = 100;
    int discount = 80;  // 80% = 打 8 折
    double final_price = price * discount / 100;
    printf("Bug 2: 折扣價 = %.1f (應該是 80.0)\n", final_price);
    
    // ========================================
    // Bug 3：華氏轉攝氏
    // 公式：C = (F - 32) * 5 / 9
    // 華氏 100 度應該是攝氏 37.78 度左右
    // ========================================
    int fahrenheit = 100;
    double celsius = (fahrenheit - 32) * 5 / 9;
    printf("Bug 3: %d°F = %.2f°C (應該約 37.78)\n", fahrenheit, celsius);
    
    // ========================================
    // Bug 4：判斷是否為偶數
    // 10 應該被判斷為偶數
    // ========================================
    int number = 10;
    int is_even = number % 2 = 0;  // 這行有語法錯誤
    printf("Bug 4: %d 是偶數嗎？%d (應該是 1)\n", number, is_even);
    
    // ========================================
    // Bug 5：計算總秒數
    // 2 小時 30 分 45 秒 = 9045 秒
    // ========================================
    int hours = 2, minutes = 30, seconds = 45;
    int total_seconds = hours * 60 + minutes * 60 + seconds;
    printf("Bug 5: 總秒數 = %d (應該是 9045)\n", total_seconds);
    
    return 0;
}

/*
 * 請在下方寫出你找到的錯誤和修正方式：
 * 
 * Bug 1: 
 * 錯誤原因：
 * 修正方式：
 * 
 * Bug 2:
 * 錯誤原因：
 * 修正方式：
 * 
 * Bug 3:
 * 錯誤原因：
 * 修正方式：
 * 
 * Bug 4:
 * 錯誤原因：
 * 修正方式：
 * 
 * Bug 5:
 * 錯誤原因：
 * 修正方式：
 */
