# Lab 03 Part 2: 優先順序追蹤答案

請追蹤每個表達式的計算過程。

## 運算子優先順序參考

| 優先順序 | 運算子 | 說明 |
|---------|-------|------|
| 1 (最高) | () | 括號 |
| 2 | ++ -- | 遞增遞減 |
| 3 | * / % | 乘除餘 |
| 4 | + - | 加減 |
| 5 | = += -= | 賦值 |

---

## 題目 1: `2 + 3 * 4`

計算過程：
```
第一步：
第二步：
```

**結果**：___

---

## 題目 2: `(2 + 3) * 4`

計算過程：
```
第一步：
第二步：
```

**結果**：___

---

## 題目 3: `10 - 4 / 2 + 1`

計算過程：
```
第一步：
第二步：
第三步：
```

**結果**：___

---

## 題目 4: `17 % 5 + 3 * 2`

計算過程：
```
第一步：
第二步：
第三步：
```

**結果**：___

---

## 題目 5: `2 + 3 * 4 - 6 / 2`

計算過程：
```
第一步：
第二步：
第三步：
第四步：
```

**結果**：___

---

## 題目 6: 連續賦值

```c
a = b = c = 5;
a = a + b + c;
```

追蹤：
```
執行 a = b = c = 5 後：a = ___, b = ___, c = ___
執行 a = a + b + c 後：a = ___
```

**最終 a 的值**：___

---

## 題目 7: `x++ + ++x` (x 初始值為 3)

這題比較複雜，請仔細追蹤：

| 步驟 | 運算 | x 的值 | 說明 |
|-----|------|-------|------|
| 初始 | - | 3 | |
| x++ | | | 先用(3)再加 |
| ++x | | | 先加再用 |
| 相加 | | | |

**結果**：___
**最終 x**：___

⚠️ 注意：這種寫法會導致「未定義行為」，實際程式中應該避免！

---

## 題目 8: `(y = 10)` 的值

**結果**：___

**這是賦值還是比較？**



**為什麼賦值表達式有值？**


